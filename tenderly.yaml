account_id: ""
actions:
  jordaniza/project:
    runtime: v2
    sources: actions
    specs:
      first-action:
        description: I wrote this to test the YAML Spec.
        function: blockNumberLogger:logger
        trigger:
          type: block
          block:
            network:
              - 1
            blocks: 10
      on-admin-change:
        description: "Fires when the admin changes"
        function: onAdminChange:newAdmin
        trigger:
          type: transaction
          transaction:
            status:
              - mined
            filters:
              - network: 43113 # avax fuji
                eventEmitted:
                  contract:
                    address: "0x9901d0111712b1e9962275c313ca67472ecf7afb"
                  name: AdminSet

project_slug: ""
